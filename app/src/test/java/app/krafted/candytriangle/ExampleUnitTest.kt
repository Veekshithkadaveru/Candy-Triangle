package app.krafted.candytriangle

import org.junit.Assert.assertTrue
import org.junit.Test

/**
 * Placeholder smoke test proving the JVM unit-test source set compiles and runs.
 *
 * Replaced in C2 by the real verification suite (§11: determinism, §3.3 clearance,
 * level feasibility, tunnelling).
 */
class ExampleUnitTest {
    @Test
    fun jvmTestSourceSetRuns() {
        assertTrue(true)
    }
}
